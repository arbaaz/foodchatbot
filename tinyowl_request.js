var Promise = require('promise');
const config = require('./config.js').settings;

// GET /dishes from locality_id
function get_dishes(locality_id){

}

// POST /login with number
function login_with_number(number){

}

// POST /verify with otp
function verify_with_otp(pre_token, otp){

}

// GET /addresses with session_token
function get_addresses(session_token){

}

// POST /address
function post_address(session_token, address){

}

// POST /order
function place_order(session_token, items){

}
